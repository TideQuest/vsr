import { CreateClaimOnMechainOpts } from '../../mechain/types';
import { ClaimTunnelResponse } from '../../proto/api';
import { ProviderName } from '../../types';
/**
 * Creates a Reclaim claim on the AVS chain.
 */
export declare function createClaimOnMechain<N extends ProviderName>({ createClaimOnAttestor, onStep, client, ...opts }: CreateClaimOnMechainOpts<N>): Promise<{
    taskId: number;
    responses: ClaimTunnelResponse[];
}>;
