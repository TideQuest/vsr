# Database Configuration
POSTGRES_USER=zksteam
POSTGRES_PASSWORD=password123
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=zksteam_db
# For Docker Compose (uses service name 'db' as host):
DATABASE_URL=postgresql://zksteam:password123@db:5432/zksteam_db
# For local development (if running PostgreSQL locally):
# DATABASE_URL=postgresql://zksteam:password123@localhost:5432/zksteam_db

# Ollama Configuration
OLLAMA_MODEL=mistral:7b-instruct-q4_K_M
OLLAMA_BASE_URL=http://ollama:11434

# Reclaim Protocol Configuration
# Get these from https://dev.reclaimprotocol.org/
RECLAIM_APP_ID=your_reclaim_app_id_here
RECLAIM_APP_SECRET=your_reclaim_app_secret_here
RECLAIM_PROVIDER_ID=your_provider_id_here

# ZKP Configuration
# Set to false for production mode with real Reclaim proofs
ZKP_MOCK=true

# Server Configuration
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173,http://localhost:8080

# Frontend Configuration
VITE_BACKEND_URL=http://localhost:3000
VITE_WALLETCONNECT_PROJECT_ID=
